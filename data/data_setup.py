from uci_data import UCIData
from augmentation import augment_features

dataset = UCIData()
num_aug = 1  # Number of feature vectors to augment
# print(f"Augmenting {num_aug} feature vector(s) from UCI Heart Disease dataset.")
# augmented_data = augment_features(dataset.get_features(), num_aug=1)

